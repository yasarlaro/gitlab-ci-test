stages:
  - build
  - test

build:
  stage: build
  script:
    - ip a
    - sudo yum install wget -y
    - cat /etc/*release
    - hostname
    - date
    - pwd

test:
  stage: test
  script:
    - echo "testing custom runner"